const BASE_COLORS = {
  gray: "rgba(0, 10, 23, 1)",
  white: "rgba(255, 255, 255, 1)",
};

const theme = {
  body: {
    background: BASE_COLORS.gray,
    color: BASE_COLORS.white,
  },
};

export default theme;
